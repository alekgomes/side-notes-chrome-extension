{
  "name": "side-notes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",    
    "build-extension": "vite build && tsc src/content_script.ts && cp src/content_script.js src/service-worker.js src/manifest.json dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "@types/chrome": "^0.0.237",
    "preact": "^10.13.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "typescript": "^5.0.2",
    "vite": "^4.3.9"
  }
}
